<h1>Article</h1>
  <h2><%= @article.title %></h2>
  <p><%= @article.content %></p>

<%= link_to 'Voltar para o menu', articles_path %>
<%= link_to 'Editar artigo', edit_article_path %>
<%= link_to 'Deletar artigo', article_path,
    method: :delete,
    data: { confirm: "Are you sure?" } %>
